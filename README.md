# smartharvester
End-to-end CI/CD: Cloud9 → private git → CodeBuild builds/tests and publishes a Python package to PyPI, creates artifacts stored in S3, deploys to Elastic Beanstalk backed by RDS and ElastiCache, with CloudWatch monitoring and SNS alerts.
